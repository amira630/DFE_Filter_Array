function Hd_fir_comp = FIR_comp
%FIR_COMP Returns a discrete-time filter object.

% MATLAB Code
% Generated by MATLAB(R) 9.10 and DSP System Toolbox 9.12.
% Generated on: 25-Oct-2025 11:22:20

% Equiripple Lowpass filter designed using the FIRPM function.

% All frequency values are normalized to 1.

Fpass = 0.8;               % Passband Frequency
Fstop = 0.9;               % Stopband Frequency
Dpass = 0.028774368332;    % Passband Ripple
Dstop = 0.00031622776602;  % Stopband Attenuation
dens  = 19;                % Density Factor

% Calculate the order from the parameters using FIRPMORD.
[N, Fo, Ao, W] = firpmord([Fpass, Fstop], [1 0], [Dpass, Dstop]);

% Calculate the coefficients using the FIRPM function.
b  = firpm(N, Fo, Ao, W, {dens});
Hd_fir_comp = dfilt.dffir(b);
% Set the arithmetic property.
set(Hd_fir_comp, 'Arithmetic', 'fixed', ...
    'CoeffWordLength', 16, ...
    'CoeffAutoScale', false, ...
    'NumFracLength', 15, ...
    'Signed',         true, ...
    'InputWordLength', 16, ...
    'inputFracLength', 15, ...
    'FilterInternals',  'SpecifyPrecision', ...
    'OutputWordLength',  16, ...
    'OutputFracLength',  15, ...
    'ProductWordLength', 32, ...
    'ProductFracLength', 30, ...
    'AccumWordLength',   40, ...
    'AccumFracLength',   30, ...
    'RoundMode',         'ceil', ...
    'OverflowMode',      'Saturate');
normalize(Hd_fir_comp);


% [EOF]
