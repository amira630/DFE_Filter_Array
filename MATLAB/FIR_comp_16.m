function Hd_FIR_comp_16 = FIR_comp_16
%FIR_COMP_16 Returns a discrete-time filter object.

% MATLAB Code
% Generated by MATLAB(R) 25.2 and DSP System Toolbox 25.2.
% Generated on: 19-Nov-2025 04:45:41

% Equiripple Lowpass filter designed using the FIRPM function.

% All frequency values are in Hz.
Fs = 375000;  % Sampling Frequency

Fpass = 93750;             % Passband Frequency
Fstop = 131250;            % Stopband Frequency
Dpass = 0.011512416822;    % Passband Ripple
Dstop = 0.00031622776602;  % Stopband Attenuation
dens  = 20;                % Density Factor

% Calculate the order from the parameters using FIRPMORD.
[N, Fo, Ao, W] = firpmord([Fpass, Fstop]/(Fs/2), [1 0], [Dpass, Dstop]);

% Calculate the coefficients using the FIRPM function.
b  = firpm(N, Fo, Ao, W, {dens});
Hd_FIR_comp_16 = dfilt.dffir(b);
% Set the arithmetic property.
set(Hd_FIR_comp_16, 'Arithmetic', 'fixed', ...
    'CoeffWordLength', 20, ...
    'CoeffAutoScale', false, ...
    'NumFracLength', 18, ...
    'Signed',         true, ...
    'InputWordLength', 16, ...
    'inputFracLength', 15, ...
    'FilterInternals',  'SpecifyPrecision', ...
    'OutputWordLength',  16, ...
    'OutputFracLength',  15, ...
    'ProductWordLength', 36, ...
    'ProductFracLength', 33, ...
    'AccumWordLength',   40, ...
    'AccumFracLength',   33, ...
    'RoundMode',         'convergent', ...
    'OverflowMode',      'Saturate');
denormalize(Hd_FIR_comp_16);


% [EOF]
