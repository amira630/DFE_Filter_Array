function Hd_Fractional_Decimator = Fractional_Decimator
%FRACTIONAL_DECIMATOR_TEST Returns a multirate filter object.

% MATLAB Code
% Generated by MATLAB(R) 25.2 and DSP System Toolbox 25.2.
% Generated on: 17-Dec-2025 01:56:59

% Equiripple Lowpass filter designed using the FIRPM function.

% All frequency values are in Hz.
Fs = 18000000;  % Sampling Frequency

Fpass = 2800000;           % Passband Frequency
Fstop = 3200000;           % Stopband Frequency
Dpass = 0.014390163418;    % Passband Ripple
Dstop = 4.4668359215e-05;  % Stopband Attenuation
dens  = 20;                % Density Factor

% Calculate the order from the parameters using FIRPMORD.
[N, Fo, Ao, W] = firpmord([Fpass, Fstop]/(Fs/2), [1 0], [Dpass, Dstop]);

% Calculate the coefficients using the FIRPM function.
b  = firpm(N, Fo, Ao, W, {dens});
intf = 2;                     % Interpolation Factor
decf = 3;                     % Decimation Factor

Hd_Fractional_Decimator = dsp.FIRRateConverter( ...
    'FullPrecisionOverride', false, ...
    'InterpolationFactor', intf, ...
    'DecimationFactor', decf, ...
    'Numerator', b, ...
    'CoefficientsDataType', 'Custom', ...
    'CustomCoefficientsDataType', numerictype([],20,18), ...
    'ProductDataType', 'Custom', ...
    'CustomProductDataType', numerictype([],36,33), ...
    'AccumulatorDataType', 'Custom', ...
    'CustomAccumulatorDataType', numerictype([],43,33), ...
    'OutputDataType', 'Custom', ...
    'CustomOutputDataType', numerictype([],16,15), ...
    'RoundingMethod', 'Convergent', ...
    'OverflowAction', 'Saturate');

% [EOF]
