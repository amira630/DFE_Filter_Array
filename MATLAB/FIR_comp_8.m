function Hd_FIR_comp_8 = FIR_comp_8
%FIR_COMP_8 Returns a discrete-time filter object.

% MATLAB Code
% Generated by MATLAB(R) 25.2 and DSP System Toolbox 25.2.
% Generated on: 16-Nov-2025 00:23:33

% Equiripple Lowpass filter designed using the FIRPM function.

% All frequency values are in Hz.
Fs = 750000;  % Sampling Frequency

Fpass = 21875;           % Passband Frequency
Fstop = 71875;           % Stopband Frequency
Dpass = 0.028774368332;  % Passband Ripple
Dstop = 0.0001;          % Stopband Attenuation
dens  = 20;              % Density Factor

% Calculate the order from the parameters using FIRPMORD.
[N, Fo, Ao, W] = firpmord([Fpass, Fstop]/(Fs/2), [1 0], [Dpass, Dstop]);

% Calculate the coefficients using the FIRPM function.
b  = firpm(N, Fo, Ao, W, {dens});
Hd_FIR_comp_8 = dfilt.dffir(b);
% Set the arithmetic property.
set(Hd_FIR_comp_8, 'Arithmetic', 'fixed', ...
    'CoeffWordLength', 20, ...
    'CoeffAutoScale', false, ...
    'NumFracLength', 18, ...
    'Signed',         true, ...
    'InputWordLength', 16, ...
    'inputFracLength', 15, ...
    'FilterInternals',  'SpecifyPrecision', ...
    'OutputWordLength',  16, ...
    'OutputFracLength',  15, ...
    'ProductWordLength', 36, ...
    'ProductFracLength', 33, ...
    'AccumWordLength',   41, ...
    'AccumFracLength',   33, ...
    'RoundMode',         'convergent', ...
    'OverflowMode',      'Saturate');
denormalize(Hd_FIR_comp_8);


% [EOF]
